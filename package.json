{
  "name": "taskflow-root",
  "private": true,
  "scripts": {
    "prepare": "uv sync --directory api",
    "dev": "concurrently -n ui,api -c blue,green \"pnpm --filter ui dev\" \"uv run --directory api uvicorn app.main:app --host 127.0.0.1 --port 8000 --reload\"",
    "lint": "concurrently -n ui,api -c blue,green \"pnpm --filter ui lint\" \"uv run --directory api ruff check .\"",
    "format": "concurrently -n ui,api -c blue,green \"pnpm --filter ui format\" \"uv run --directory api ruff format . && uv run --directory api ruff check --fix .\"",
    "typecheck": "concurrently -n ui,api -c blue,green \"pnpm --filter ui typecheck\" \"uv run --directory api pyright .\"",
    "build": "pnpm --filter ui build"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}